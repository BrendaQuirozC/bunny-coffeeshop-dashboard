
  # Data Visualization Dashboard

  This is a code bundle for a Data Visualization Dashboard of a coffe shop. It shows data analitics to help the bussiness with better decitions.

  It was designed using figma and developer with React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  